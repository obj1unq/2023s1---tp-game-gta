import wollok.game.*
import crash.*

program juego {
	//tablero
	game.title("Crash")
	game.height(15)
	game.width(21)
	game.boardGround('campo.jpg')
	//personajes y acciones
	//game.addVisual(crash)
	//game.addVisual(pared)

	game.start()
}